Mixture Sparse Input Neural Network

The program is built with `Equinox` and `JAX`

* make train dataset run `bash make_dataset.sh`
* run linear or nonlinear simulation run `bash linear.sh` or `bash nonlinear.sh`